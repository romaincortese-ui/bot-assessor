[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bot-assessor scheduled"
cronSchedule = "0 */6 * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 2